#include <bits/stdc++.h>

using namespace std;

int main() {

    // ! General Foramt:
    // * [](int a) -> int { return a - 1; };
    // [] is start of lambda
    // (int a) is Parameter list
    // -> int is return type
    // { return a - 1; } is body

    // [&]:     capture all external variables by reference.
    // []:      capture all external variables by value.
    // [a, &b]: capture 'a' by value and 'b' by reference.
}